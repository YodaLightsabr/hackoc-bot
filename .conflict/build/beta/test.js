import Flow from '@conflict/beta/flow';
import { useState } from '@conflict/beta';
export default class Text extends Flow {
  constructor(invoker, options) {
    super(invoker, options);
  }

  next() {}

}